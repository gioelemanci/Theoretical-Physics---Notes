\section{Integrals in Dimensional Regularization}

 [...]

\[
    I = \int \mathrm{d}^D k f(k) \implies \mathrm{d}^D k f(k) \big|_{k^{\mu} \to \pm \infty} = 0.
\]

This has some consequences.
\begin{itemize}
    \item loop integrals...
    \item integration by parts...
    \item scaleless integrals vanish.
          Let us say that we have \([f(k)] = \Delta_f\) an integer, which means that any dimensional quantity must be set to zer, except for the loop momentum \(k\). Then, we have
          \[
              \int \mathrm{d}^D k f(k) = \lambda^{D + \Delta_f} \int \mathrm{d}^D k f(k),
          \]
          as we scale \(k \to \lambda k\). This can only be satisfied if the integral vanishes, since \(\lambda^{D + \Delta_f} = 1\) only if \(D + \Delta_f = 0\), such that in general it is not the case.
\end{itemize}
\begin{example}
    \[
        \int \mathrm{d}^D k \frac{1}{(k^2)^{\alpha} } = 0,
    \]
    for any \(\alpha\), since the integrand is scaleless. Thus from integration by parts we have
    \[
        \begin{aligned}
            \dots \dots
        \end{aligned}
    \]
    By analytic continuation...
\end{example}

\begin{example}
    \[
        \int \mathrm{d}^D k \frac{1}{(k^2 - M^2)^{\alpha}} = 0,
    \]
    but only if \(\alpha \leq 0\):
    \begin{itemize}
        \item (\(\alpha=0\)): ...
        \item (\(\alpha = -1\)): ...
    \end{itemize}
\end{example}
Thus dimensional regularization defines how we treat scaleless integrals, which is a very convenient feature. If the integral diverges, we can consistently ..........

\subsection{Integrals with Numerators}

In most theories, momenta and loop moemnta can appear in the numerator of the integrand, and we need to know how to deal with them.

    [...]

\subsubsection{Dirac Algebra in \(D\) Dimensions}

In dimensional regularization, we need to define the Dirac algebra in \(D\) dimensions.

We can consistently restrict the fermions to have only two polarizations (plus two for antifermions), ...

    [\dots]

Anticommutation relations are almost the same, but now we are considering a \(D\)-dimensional \(g^{\mu \nu}\):
\[
    \{\gamma^{\mu}, \gamma^{\nu}\} = 2 g^{\mu \nu} \times \mathbb{I}_4.
\]
where the identity is still \(4 \times 4\) because we are keeping the number of polarizations fixed. We can also define the trace of two gamma matrices as
\[
    \dots
\]

All trace identities we found are still valid up to the promotion of the metric to \(D\) dimensions, and the fact that the trace of the identity is still 4. However we have
\[
    \gamma_\mu \gamma^\mu = \frac{1}{2} \dots = g_\mu^{\mu} \times \mathbb{I}_4 = D \times \mathbb{I}_4,
\]
\begin{example}[exercise]
    We already computed it in four dimensions, but we can also compute it in \(D\) dimensions:
    \[
        \gamma^{\mu} \gamma^{\nu} \gamma^{\rho} \gamma^{\sigma} \gamma_{\mu} = \dots
    \]
\end{example}